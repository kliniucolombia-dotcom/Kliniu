import AccountEntryLoading from "./components/account-entry-loading";

export default function Loading() {
  return (
    <AccountEntryLoading
      message="Cargando"
      detail="Un momento, estamos preparando todo."
    />
  );
}
