import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Usuarios — Panel Comercial",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
