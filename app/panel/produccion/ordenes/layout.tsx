import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Órdenes de Producción — Panel Comercial — Kliniu",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
