import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Departamentos — Panel Comercial — Kliniu",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
