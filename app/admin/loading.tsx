import AccountEntryLoading from "../components/account-entry-loading";

export default function Loading() {
  return (
    <AccountEntryLoading
      message="Abriendo el panel"
      detail="Cargando accesos, productos y herramientas de administración."
    />
  );
}
